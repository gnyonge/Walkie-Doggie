<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.pet.dao.HealtDao">

	<insert id="insert" parameterType="HealthDto">
		insert into
		health(hid, peid,
		h_content, h_date, h_flag)
		values(#{hid},
		#{peid},
		#{h_content},
		${h_date}, 0)
	</insert>

</mapper>